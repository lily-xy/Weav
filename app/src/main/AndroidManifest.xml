<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weav">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Weav">
        <activity android:name=".PersonActivity"></activity>
        <activity android:name=".buildingActivity" />
        <activity android:name=".succeedActivity" />
        <activity android:name=".previewActivity" />
        <activity android:name=".getFilenameActivity" />
        <activity android:name=".ScanActivity" />
        <activity android:name=".DetailDataActivity" />
        <activity android:name=".ContentActivity" />
        <activity android:name=".CameraActivity" />
        <activity android:name=".HueActivity" />
        <activity android:name=".EffectActivity" />
        <activity android:name=".DealActivity" />
        <activity android:name=".SelectionActivity" />
        <activity android:name=".GalleryActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>